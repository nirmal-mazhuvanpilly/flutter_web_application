import 'package:flutter/material.dart';

class PaddingConsts {
  static const commonPadding10 = EdgeInsets.all(10);
  static const commonPadding15 = EdgeInsets.all(15);
  static const commonPadding20 = EdgeInsets.all(20);
  static const commonPadding25 = EdgeInsets.all(25);

  static const verticalPadding10 = EdgeInsets.symmetric(vertical: 10);
  static const verticalPadding20 = EdgeInsets.symmetric(vertical: 20);

  static const leftRightTop25 = EdgeInsets.only(left: 25, right: 25, top: 25);

  static const leftRight25Top10 = EdgeInsets.only(left: 25, right: 25, top: 10);

  static const vertical20horizontal25 =
      EdgeInsets.symmetric(horizontal: 25, vertical: 20);

  static const left40 = EdgeInsets.only(left: 40);
}
